package week12.day3;

// Gson이나 Jackson과 같은 JSON 처리 라이브러리의 역할을 설명하는 예제입니다.
public class Answer5 {
    // 메인 메서드
    public static void main(String[] args) {
        /*
         * [JSON 파싱 라이브러리(Gson, Jackson)의 필요성과 역할]
         * 
         * 위 예제들처럼 문자열을 직접 조작(정규식 등)하여 JSON을 다루는 것은 
         * 구조가 복잡해지면 버그가 발생하기 쉽고 한계가 있습니다.
         * 
         * Gson (Google 개발), Jackson (Spring 기본 탑재) 등의 라이브러리는:
         * 1. 자바 객체(DTO, VO)를 JSON 문자열로 자동 변환(Serialization, 직렬화)해주거나,
         * 2. JSON 문자열을 파싱하여 자바 객체 필드에 매핑(Deserialization, 역직렬화)하는
         * 작업을 매우 쉽고 안정적으로 도와주는 핵심 라이브러리입니다.
         */
    }
}
